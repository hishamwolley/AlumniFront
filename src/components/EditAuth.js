import React from "react";
import { Card } from "react-bootstrap";

const EditAuth = () => {
	return (
		<Card className="text-center">
			<Card.Body>
				<h2 className="text-danger">Not authroized</h2>
			</Card.Body>
		</Card>
	);
};

export default EditAuth;
